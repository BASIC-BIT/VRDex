import type { ReactNode } from "react";

// Invitation recipients are not staff yet. Only (workspace) loads private data.
export default function CommunityLayout({ children }: { children: ReactNode }) {
  return children;
}
